Install Requirements, Update config.py, Run queries.sql
```
sudo apt-get install python-pip
pip install flask
sudo apt-get install mysql-server libmysqlclient-dev
pip install flask-mysqldb
pip install Flask-WTF
pip install passlib
```
